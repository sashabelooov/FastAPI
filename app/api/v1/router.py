from fastapi import APIRouter

# Import and include feature routers here as you add them:
# from app.api.v1 import users
# router.include_router(users.router, prefix="/users", tags=["users"])

router = APIRouter()
